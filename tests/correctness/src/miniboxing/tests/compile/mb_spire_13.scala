package miniboxing.tests.correctness
import miniboxing.plugin.minispec
import scala.annotation.tailrec

object LinearSelect {
  final def approxMedian[@minispec A](data: Array[A]): A = {
    data(0)
  }
}

